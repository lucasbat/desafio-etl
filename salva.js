//import api from "./api";
/*
import Axios from "axios";
const PORT = process.env.PORT || 3001;
//const express = require("express");
import express from 'express';
const app = express();

const requireData = async () => {
  let result1 = await Axios.get(`http://challenge.dienekes.com.br/api/numbers?page=${page}`);
  console.log("result1",result1);
  for await(const page of )
  let todos
    let page = 1;
    //while (response ) {
      
      //response.data.numbers.length 
    //}
    try{
      while (page <= 10000) {
        response = await Axios.get(`http://challenge.dienekes.com.br/api/numbers?page=${page}`);
        console.log("page",page);
        //console.log("response.data.numbers.length =>", response.data.numbers.length);  
        //console.log("response==>", response);  
        page++;
      }
    }
    catch (error){
        console.log("Deu ruim");
    }
      //console.log(response.data.numbers.length);
   // this.setState({ filmes: response.data });
  }

  async function groupData() {
    const dados = requireData();
    //console.log("dados", dados); 

  }
  groupData();
   
  




app.listen(PORT, () => {
  console.log(`Server listening on ${PORT}`);
})
*/